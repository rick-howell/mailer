{
  "name": "nodemailer-simple",
  "version": "1.0.0",
  "description": "Minimal static site + Nodemailer contact form, deployable on Render.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "nodemailer": "^10.0.10"
  }
}